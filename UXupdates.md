on the mobile log screen make the following updates 
* change the park selection drawers to show how many are logged for each rider
* When clicking into a park, the top headers need less text. they should be a simpler calculation card
* the ride list itself shoudl borrow styling from the plan page. use images, and build a complete card to use for each ride. 
* The avatar styling should be the same and bigger